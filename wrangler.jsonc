{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "legara-site",
  "main": "src/worker.js",
  "compatibility_date": "2025-09-27",
  "observability": {
    "enabled": true
  },
  "assets": {
    "directory": "."
  },
  "compatibility_flags": [
    "nodejs_compat"
  ],
  "triggers": {
    "crons": ["0 * * * *"]
  },
  "kv_namespaces": [
    {
      "binding": "TEAM_DATA",
      "id": "71197c42d7c54d7f8eebd7f4f8fdf5c9"
    }
  ],
  "d1_databases": [
    {
      "binding": "TRACKING",
      "database_name": "legara-tracking",
      "database_id": "804637df-8435-451a-9afb-1934f6bcee3b"
    }
  ],

  // ─── Staging Environment ──────────────────────────────────────────
  // Deploy with: npx wrangler deploy --env staging
  // Accessible at: legara-site-staging.<account>.workers.dev
  //   OR staging.golegara.com (requires DNS CNAME — see docs/staging-setup.md)
  //
  // Staging uses separate KV + D1 to avoid polluting prod data.
  // Create them once via:
  //   npx wrangler kv namespace create TEAM_DATA --env staging
  //   npx wrangler d1 create legara-tracking-staging
  // Then paste the IDs below.
  "env": {
    "staging": {
      "name": "legara-site-staging",
      "route": {
        "pattern": "staging.golegara.com/*",
        "zone_name": "golegara.com"
      },
      "kv_namespaces": [
        {
          "binding": "TEAM_DATA",
          "id": "<FILL-IN: run 'npx wrangler kv namespace create TEAM_DATA --env staging' and paste the id>"
        }
      ],
      "d1_databases": [
        {
          "binding": "TRACKING",
          "database_name": "legara-tracking-staging",
          "database_id": "<FILL-IN: run 'npx wrangler d1 create legara-tracking-staging' and paste the id>"
        }
      ]
    }
  }
}
